import type { MigrationInterface, QueryRunner } from 'typeorm';

/**
 * Уточнение дефолтного промпта отбора по названию (§4.12.2).
 *
 * Прежняя редакция проверялась замером на 40 реальных названиях с выдачи hh.ru:
 * `qwen3:4b-instruct` терял 4 подходящие вакансии из 40 — все те, где стек в названии
 * назван не тот («AI fullstack product engineer», «.NET + React»), — а `qwen3:1.7b`
 * пропускал вакансии тестировщиков и аналитиков со словом fullstack в названии.
 * Новая редакция называет признак прямо («сам пишет код продукта») и перечисляет
 * отклоняемые специальности поимённо: на том же наборе крупная модель теряет одну
 * вакансию вместо четырёх и отвечает на 23 % быстрее.
 *
 * Промпт правится пользователем на фронте (§7.9.4), поэтому UPDATE идёт с условием
 * на прежний текст: изменённый вручную промпт эта миграция не трогает. По той же
 * причине down() возвращает старый текст только там, где сейчас стоит ровно новый —
 * иначе revert затёр бы правку пользователя (та же ошибка, что разбиралась
 * в RepairGetmatchVacancySource).
 *
 * ОСОЗНАННОЕ ИСКЛЮЧЕНИЕ из §10 п.3 («никаких литералов в имплементационных файлах»):
 * миграция — неизменяемый снимок данных на конкретный момент, см. подробное
 * обоснование в CreateVacancyLeadsTable.
 */
export class SharpenVacancyTitlePrompt1786800000000 implements MigrationInterface {
  private readonly previousPrompt =
    'Ты помогаешь отбирать вакансии. Ключевые слова профиля: {keywords}.\n' +
    'Для каждого названия вакансии реши, соответствует ли оно этому профилю.\n' +
    'Учитывай синонимы и родственные технологии, а не только буквальные совпадения.\n' +
    'Отклоняй другие специальности, даже если слово из списка встретилось случайно.\n' +
    'Названия:\n' +
    '{titles}\n' +
    'Ответь JSON-массивом по одному объекту на каждое название, в том же порядке.';

  private readonly sharpenedPrompt =
    'Ты помогаешь отбирать вакансии для разработчика. Ключевые слова профиля: {keywords}.\n' +
    'Подходит только тот, кто сам пишет код продукта: разработчик, программист, developer, engineer.\n' +
    'Отклоняй любые другие специальности, даже если в названии есть слово fullstack:\n' +
    'тестировщика и QA, аналитика (системного, бизнес, любого), менеджера, дизайнера,\n' +
    'devops, преподавателя, руководителя без разработки.\n' +
    'Разработчик подходит и тогда, когда его основной язык другой, но в названии есть\n' +
    'React, Node.js, TypeScript или JavaScript.\n' +
    'Разработчик подходит, если название не уточняет стек вовсе.\n' +
    'Названия:\n' +
    '{titles}\n' +
    'Ответь JSON-массивом по одному объекту на каждое название, в том же порядке.';

  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `UPDATE "vacancy_search_settings"
         SET "title_prompt" = $1
       WHERE "title_prompt" = $2`,
      [this.sharpenedPrompt, this.previousPrompt],
    );
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `UPDATE "vacancy_search_settings"
         SET "title_prompt" = $1
       WHERE "title_prompt" = $2`,
      [this.previousPrompt, this.sharpenedPrompt],
    );
  }
}
