/**
 * Логотип компании (§4.10) — общие константы для откликов и найденных вакансий.
 * Вынесены сюда из constants/application.constants.ts: используются обоими экранами,
 * а не только откликами.
 */

/** Буква-фолбэк логотипа, когда у company нет непробельного символа. */
export const COMPANY_INITIAL_FALLBACK = '?';
